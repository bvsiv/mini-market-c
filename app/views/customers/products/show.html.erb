<%= @product.name %>
<%= @product.description %>
Merchant: <%= @product.merchant.name %>
Contact: <%= @product.merchant.email %>

<% @product.offers.each do |offer| %>
  <%= number_to_currency(offer.suggested_price, unit:"zł", separator: ",", delimiter: " ", format: "%n %u") %>
  offered by: <%= offer.customer.name %>
<% end %>

<%= link_to 'New Offer', customers_offer_new_path %>
