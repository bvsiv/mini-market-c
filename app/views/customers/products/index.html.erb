<!doctype html>
  <html lang="en">
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
      <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous"/>
      <%= favicon_link_tag 'minilogo3.png' %>
      <%= stylesheet_link_tag('bootstrap-grid')%>
      <title>Mini Market</title>
    </head>

    <body>
      <div class="preloader">
      <div class="cssload-speeding-wheel"></div>
    </div>

      <% @products.each do |product| %>
      <p><%= product.name %>
      <%= number_to_currency(product.offers.pluck(:suggested_price).max, unit:"zł", separator: ",", delimiter: " ", format: "%n %u")%></p>
      <p>from <%= product.merchant.name %></p>
      <p><%= product.description %></p>
      <a href="#" target="_blank" class="btn details btn-danger pull-right m-l-20 btn-rounded btn-outline hidden-xs hidden-sm waves-effect waves-light">See more</a>
      <% end %>

    </body>
  </html>
